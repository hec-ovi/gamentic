# Gamentic voice service - pinned to what was validated on the box (2026-06-08).
kokoro-onnx==0.5.0
onnxruntime==1.26.0
soundfile==0.14.0
numpy==2.4.6
fastapi==0.136.3
uvicorn[standard]==0.49.0
# espeakng-loader (pulled by kokoro-onnx) bundles the espeak-ng phonemizer lib,
# so no system espeak install is required.
