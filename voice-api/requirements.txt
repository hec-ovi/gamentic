# Gamentic voice service - Maya1 SNAC decode path, pinned to what was validated
# on the box (2026-06-09). torch is the CPU build (see --extra-index-url): SNAC
# decode is light and the iGPU belongs to llama.cpp/ComfyUI.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
snac==1.2.1
httpx==0.28.1
soundfile==0.14.0
numpy==2.4.6
fastapi==0.136.3
uvicorn[standard]==0.49.0
